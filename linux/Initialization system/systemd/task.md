
Практика №2 — Создание и настройка таймера systemd
В этой практике вам нужно самостоятельно создать systemd-таймер, который будет периодически запускать bash-скрипт. Выполните следующие шаги:

Шаги выполнения задания:
Создайте bash-скрипт /home/user/practice_timer.sh, который при запуске записывает текущие дату и время в файл /home/user/practice_timer.log.
Создайте два файла юнитов systemd:
Файл сервиса: /etc/systemd/system/practice_timer.service
Файл таймера: /etc/systemd/system/practice_timer.timer
Настройте таймер на запуск скрипта каждые 2 минуты.
Перезагрузите конфигурацию systemd, включите и запустите таймер.
Требования к практике:
Файл лога должен дополняться при каждом запуске таймера.
Таймер должен запускаться автоматически после перезагрузки системы.
Ожидаемый результат (после настройки и запуска таймера):
Через несколько минут после включения таймера файл /home/user/practice_timer.log должен содержать примерно следующее:

Mon Apr 14 12:30:00 UTC 2025
Mon Apr 14 12:32:00 UTC 2025
Mon Apr 14 12:34:00 UTC 2025

                  
🎯 Шпаргалка: как не запутаться с systemd timer
Что именно нужно сделать (4 шага)
Скрипт: подготовить исполняемый файл /home/user/practice_timer.sh, который добавляет дату в /home/user/practice_timer.log (оператор >>).

Сервис (.service): описать что запускать — ваш скрипт.

Таймер (.timer): описать когда запускать — каждые 2 минуты.

Перечитать конфигурацию и включить таймер: daemon-reload → enable --now.

Мини-шаблоны (не копируй бездумно — проверь пути!)
1) Скрипт

Файл: /home/user/practice_timer.sh

Смысл: одна строка добавляет текущую дату в лог.

Важно: сделать исполняемым.

Подсказка для содержимого:

#!/bin/bash 
date >> /home/user/practice_timer.log

                  
Команда прав:

chmod +x /home/user/practice_timer.sh

                  
2) Сервис — /etc/systemd/system/practice_timer.service

Секции обязательны: [Unit], [Service], [Install].

Тип сервиса: Type=oneshot (запустился — сделал дело — вышел).

Самое главное — ExecStart указывает на ваш скрипт.

Подсказка для ключевых строк:

[Service]
Type=oneshot
ExecStart=/home/user/practice_timer.sh

                  
(Секции [Unit]/[Install] тоже нужны, но главное здесь — ExecStart и Type.)

3) Таймер — /etc/systemd/system/practice_timer.timer

Секции обязательны: [Unit], [Timer], [Install].

Ключевая строка расписания: каждые 2 минуты через OnCalendar.

Нужно связать таймер с сервисом через Unit=practice_timer.service.

В установке таймера обычно указывают WantedBy=timers.target.

Подсказка для ключевых строк:

[Timer]
OnCalendar=*-*-* *:*:00/2
Unit=practice_timer.service

                  
⚠️ Не путай OnCalendar с cron-синтаксисом. Для systemd правильно: *-*-* *:*:00/2.

Активируем
После сохранения обоих юнитов:

sudo systemctl daemon-reload
sudo systemctl enable --now practice_timer.timer

                  
Как быстро проверить себя
Таймер в списке активных:

systemctl list-timers | grep practice_timer.timer

Статус таймера:

sudo systemctl status practice_timer.timer

Лог скрипта (через пару минут должны появляться новые строки):

cat /home/user/practice_timer.log

Логи сервиса (если нужно):

journalctl -u practice_timer.service -n 20 --no-pager

🔎 Обратите внимание
ExecStart должен указывать ровно на /home/user/practice_timer.sh.

Скрипт должен быть исполняемым (chmod +x).

В таймере обязательно укажи Unit=practice_timer.service.

После правок юнитов обязательно sudo systemctl daemon-reload.

В .timer секция [Install] обычно содержит WantedBy=timers.target, иначе автозапуск таймера после ребута не включится.

✅ Самопроверка 
 Скрипт существует и исполняется: /home/user/practice_timer.sh + chmod +x.

 В скрипте используется >> (добавление, не перезапись).

 В .service есть Type=oneshot и верный ExecStart=/home/user/practice_timer.sh.

 В .timer есть OnCalendar=*-*-* *:*:00/2 и Unit=practice_timer.service.

 Выполнены daemon-reload и enable --now practice_timer.timer.

 В list-timers таймер виден, в логе появляются новые строки каждые ~2 минуты.

🧯 Частые ошибки и как их избежать
Забыли daemon-reload → systemd не видит новые юниты. Решение: выполните sudo systemctl daemon-reload.

Неисполняемый скрипт → сервис падает. Решение: chmod +x.

Неправильный путь в ExecStart → «скрипт не найден». Проверь путь.

Нет связки таймер↔сервис (Unit=) → таймер тикает, а сервис не запускается. Добавьте Unit=practice_timer.service.

Путают crontab-синтаксис с OnCalendar → используйте формат из подсказки выше.