
Практика — Управление приоритетом процесса
Создайте исполняемый Bash-скрипт priority_demo.sh, который выполнит следующие действия:

Запустит в фоновом режиме процесс sleep 300 с nice-приоритетом +15.
Выведет на экран PID и текущий приоритет (nice) запущенного процесса.
Изменит приоритет запущенного процесса на +5, выведет сообщение: "Приоритет изменён".
Повторно выведет на экран текущий приоритет (nice) процесса после изменения.
Завершит процесс командой kill и выведет сообщение: "Процесс завершён".
Требования:

Используйте команды nice, renice и ps.
Вывод должен соответствовать приведённому ниже примеру.
Пример работы скрипта:

./priority_demo.sh
Запущен процесс PID: 5432 с приоритетом (nice): 15
Приоритет изменён
Новый приоритет (nice): 5
Процесс завершён

                  
Не забудьте сделать скрипт исполняемым:

chmod +x priority_demo.sh

                  
📝 Шпаргалка для практики
nice -n 15 sleep 300 & — запустить процесс sleep 300 с приоритетом +15 в фоне.

pid=$! — сохранить PID этого процесса в переменную.

ps -o ni= -p $pid — показать текущий приоритет (nice) процесса.

renice -n 5 -p $pid — изменить приоритет на +5.

kill $pid — завершить процесс.