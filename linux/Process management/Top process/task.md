
Практика №1 — Поиск процессов по потреблению ресурсов
Создайте исполняемый Bash-скрипт top_process.sh, который выводит процесс, занимающий больше всего оперативной памяти.

Требования:

Используйте команду ps.

Нужно отсортировать процессы по памяти (%MEM) и вывести только один — верхний.

Формат вывода должен быть таким (с точками подставьте свои значения):

PID: 1234 | Процесс: firefox | Память: 12.5%

                  
Пример работы скрипта:

./top_process.sh
PID: 4321 | Процесс: chrome | Память: 8.3%

                  
Обратите внимание, что у вас вывод может отличаться, в зависимости от того, какой процесс активен.

Не забудьте сделать скрипт исполняемым:

chmod +x top_process.sh

                  
Подсказка 📝

В Linux есть команда ps -eo pid,comm,%mem --sort=-%mem — она показывает PID, имя процесса и использование памяти.

Чтобы взять только верхнюю строку (кроме заголовка), удобно применить awk 'NR==2 {...}'.

Для вывода используйте echo или printf.